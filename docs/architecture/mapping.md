# Mapping Rules

File: `src/core/mapping/mapping-rules.json`

Sections:
- widgetMap: LVGL symbol <-> React component names
- styleMap: Style key to Tailwind & LVGL style setter
- eventMap: Event bridging (lv_event_* ↔ onClick/onChange)

Future: priority, conditional rules, style token injection.
